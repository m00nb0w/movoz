Circular outlined author/user bubble. Leave both `src` and `initials` empty for the wireframe's bare pencil ring.

```jsx
<Avatar initials="MV" />
<Avatar src="/assets/author.jpg" size={32} />
<Avatar />
```
