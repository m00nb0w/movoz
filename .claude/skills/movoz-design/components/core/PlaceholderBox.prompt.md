The crossed "cover image / code sample" placeholder frame. Drop it in wherever real imagery or a media slot will eventually go.

```jsx
<PlaceholderBox label="cover image" />
<PlaceholderBox label="code sample / token exchange" ratio="4 / 3" />
<PlaceholderBox height={80} cross={false} />
```
