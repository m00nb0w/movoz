A rounded filter chip in the marker font. Use a row of them for category/topic filtering; mark the selected one `active`.

```jsx
<Pill active>All</Pill>
<Pill>OAuth 2.1</Pill>
<Pill>Token Security</Pill>
```
