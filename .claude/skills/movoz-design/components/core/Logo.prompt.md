The Movoz lockup: an outlined square holding an italic marker initial, beside the italic wordmark. Use `onDark` on the dark tool dock.

```jsx
<Logo />
<Logo size="lg" sublabel="logo + wordmark" />
<Logo onDark mark="M" />
```
