A paper sheet with a felt-tip hairline border — the base surface for article cards, panels, and tiles. Use `interactive` for clickable cards (lifts on hover).

```jsx
<Card interactive>
  <PlaceholderBox label="cover image" />
  <h3>OIDC discovery at the edge</h3>
</Card>
```
