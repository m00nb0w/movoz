Felt-tip outlined text field. Pass an `iconLeft` for search; use `pill` for the rounded search box from the site header.

```jsx
<Input pill placeholder="Search…" iconLeft={<SearchIcon/>} />
<Input placeholder="you@company.com" type="email" />
```
